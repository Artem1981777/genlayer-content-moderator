{
  "name": "genlayer-content-moderator",
  "version": "2.0.0",
  "description": "AI content moderation on GenLayer: Intelligent Contract (registry v2) with validator-consensus verdicts, staking, permissionless appeals, reputation, SDK and dApp.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "lint": "genvm-lint check contracts/registry_v2.py",
    "test": "pytest tests/direct",
    "test:report": "pytest tests/direct --junitxml=pytest-report.xml",
    "deploy": "node --env-file=.env scripts/deploy.mjs",
    "interact": "node --env-file=.env scripts/interact.mjs",
    "e2e": "node --env-file=.env scripts/test.mjs"
  },
  "dependencies": {
    "genlayer-js": "^2.0.0-rc.1",
    "viem": "^2.55.18"
  },
  "devDependencies": {
    "genlayer": "^0.39.2"
  }
}
